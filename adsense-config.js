/* Add your own AdSense details only after following ADSENSE-SETUP.md.
   No Google advertising requests are made while enabled is false.
   These are public ad identifiers, not passwords or secret keys. */
window.STUDIO_ADSENSE = Object.freeze({
  enabled: false,
  client: "", // Your actual ca-pub- followed by 16 digits.
  slot: ""    // Your actual numeric Display ad-unit slot ID.
});
